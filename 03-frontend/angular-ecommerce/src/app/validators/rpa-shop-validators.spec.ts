import { RpaShopValidators } from './rpa-shop-validators';

describe('RpaShopValidators', () => {
  it('should create an instance', () => {
    expect(new RpaShopValidators()).toBeTruthy();
  });
});
